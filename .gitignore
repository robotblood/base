.venv/
__pycache__/
*.pyc
.env
# Anchored to the repo root: a bare `data/` also matched source directories
# further down (it silently swallowed web/src/routes/admin/data).
/data/
.screenshots/
/export/
